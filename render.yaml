services:
  - type: web
    name: music-player
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /saavn-api/*
        destination: https://www.jiosaavn.com/*
      - type: rewrite
        source: /*
        destination: /index.html
